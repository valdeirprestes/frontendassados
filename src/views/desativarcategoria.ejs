<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include("includes/assets"); %>
    <!-- Se quiser fazer um CSS personalizado desta pagina-->
    <!-- <link href="/public/assets/css/meucsspersonalizado.css" rel="stylesheet"/> -->
    <title>Desativar Produto</title>  <!--Não se esqueça de alterar o nome do titulo-->
</head>
<body>
    <div class="main">
        <%- include("includes/menubar"); %>
        <div class="contents">
            <% if(locals.categoria) { %>
            <form class="class-form-desativarcategoria" method="get" action="/categoria/listar/1">
                <input type="hidden" value="<%= categoria.id %>" name="id">
                <p>Você deseja desativar a categoria <%= categoria.nome %></p>
                <button class="botao">Sim</button> <a href="/categoria/listar/1" class="botao">Não</a>
            </form>
            <% } %>
        </div>
    </div>
    <%- include("includes/footer"); %>
</body>
</html>