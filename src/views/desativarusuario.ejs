<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include("includes/assets"); %>
    <!-- Se quiser fazer um CSS personalizado desta pagina-->
    <!-- <link href="/public/assets/css/meucsspersonalizado.css" rel="stylesheet"/> -->
    <title>titulo</title>  <!--Não se esqueça de alterar o nome do titulo-->
</head>
<body>
    <div class="main">
        <%- include("includes/menubar"); %>
        <div class="contents">
            <% if(locals.usuario) { %>
            <form class="class-form-deativarusuario" method="get" action="/usuario/listar">
                <input type="hidden" value="<%= usuario.id %>" name="id">
                <p>Você deseja desativar o usuario <%= usuario.nome %></p>
                <button class="botao">Sim</button> <a href="/usuario/listar" class="botao">Não</a>
            </form>
            <% } %>
        </div>
    </div>
    <%- include("includes/footer"); %>
</body>
</html>